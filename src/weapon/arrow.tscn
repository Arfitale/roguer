[gd_scene load_steps=4 format=3 uid="uid://ccgchljolgao5"]

[ext_resource type="Texture2D" uid="uid://cmn8d4ywvjohb" path="res://assets/weapon/arrow.png" id="1_mtuqh"]

[sub_resource type="GDScript" id="GDScript_mtuqh"]
script/source = "extends Area2D

@export var speed := 400.0
@export var damage := 1
@export var lifetime := 3.0

var direction := Vector2.ZERO

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	direction = direction.normalized()
	await get_tree().create_timer(lifetime).timeout
	queue_free()

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _physics_process(delta: float) -> void:
	position += direction * speed * delta

func _on_area_entered(area: Area2D) -> void:
	_try_hit(area.owner)

func _on_body_entered(body: Node2D) -> void:
	_try_hit(body)
	
func _try_hit(target: Node2D) -> void:
	if target and target.has_method(\"take_damage\"):
		target.take_damage(damage)
		queue_free()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_mvc1o"]
size = Vector2(16, 3)

[node name="Arrow" type="Area2D"]
collision_layer = 16
collision_mask = 96
script = SubResource("GDScript_mtuqh")

[node name="Sprite2D" type="Sprite2D" parent="."]
texture = ExtResource("1_mtuqh")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(0, 0.5)
shape = SubResource("RectangleShape2D_mvc1o")

[connection signal="area_entered" from="." to="." method="_on_area_entered"]
[connection signal="body_entered" from="." to="." method="_on_body_entered"]
